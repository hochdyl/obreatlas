import {ReactElement} from "react";
import {Container, Typography} from "@mui/material";

const Loader = (): ReactElement => {
    return (
        <Container>
            <Typography>PAGE LOADING BE PATIENT</Typography>
        </Container>
    )
}
export default Loader