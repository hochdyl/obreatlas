'use client'
import {ReactElement} from "react";
import {Stack} from "@mui/material";
import {GlassPaper} from "@/components/ui/Glass";

const MetricCard = (): ReactElement => {
    return (
        <Stack direction="row" component={GlassPaper}>
        </Stack>
    )
}
export default MetricCard