import theme from "@/theme";

const glassStyleProps = {
    border: "1px solid rgba(255, 255, 255, 0.2)",
    backgroundColor: `${theme.palette.primary.main}20`,
    backdropFilter: "blur(6px)",
}
export default glassStyleProps